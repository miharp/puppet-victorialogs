{
  "puppet_task_version": 1,
  "description": "Check systemd active state and HTTP readiness of a VictoriaLogs service",
  "parameters": {
    "service_name": {
      "description": "Systemd service name to check (default: victorialogs-single)",
      "type": "Optional[String[1]]"
    },
    "port": {
      "description": "HTTP port to probe the /-/ready endpoint on (default: 9428)",
      "type": "Optional[Integer]"
    }
  },
  "implementations": [
    {
      "name": "status.sh",
      "requirements": ["shell"]
    }
  ]
}
